# Agent Configuration
# Generated by Agent Smith
# "The best thing about being me... there are so many of me."

name: root
description: Main agent orchestrating repository assimilation and CLI lifecycle.
version: "1.0"

# Skills this agent can use
skills:
  - assimilation-pipeline-orchestration

# Tools this agent can execute
tools:
  - name: build
    command: "npm run build"
    description: "Build the project (tsc to dist)"
  - name: dev
    command: "npm run dev"
    description: "Run CLI in dev mode with tsx"
  - name: start
    command: "npm run start"
    description: "Run built CLI from dist"
  - name: lint
    command: "npm run lint"
    description: "Lint source files"
  - name: test
    command: "npm run test"
    description: "Run tests with vitest"

# Keywords that activate this agent
triggers:
  - "main"
  - "primary"
  - "agentsmith"

# Agent hierarchy
isSubAgent: false
subAgents:
  - pipeline
  - cli
