# Agent Configuration
# Generated by Agent Smith
# "The best thing about being me... there are so many of me."

name: root
description: Root agent for Jpisnice/shadcn-ui-mcp-server
version: "1.0"

# Skills this agent can use
skills:
  - src-patterns

# Tools this agent can execute
tools:
  - name: install
    command: "npm install"
    description: "Install dependencies"
  - name: build
    command: "npm run build"
    description: "Build"
  - name: test
    command: "npm test"
    description: "Run tests"

# Keywords that activate this agent
triggers:
  - "typescript"

# Agent hierarchy
isSubAgent: false
